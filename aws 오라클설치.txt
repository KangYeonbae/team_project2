sudo yum update -y
sudo yum install docker -y
docker -v

도커 명령어가 동작하지 않는다면
도커 구동

sudo service docker start
sudo systemctl start docker
sudo systemctl enable docker

sudo docker login

sudo docker search oracle-xe-11g
sudo docker pull jaspeen/oracle-xe-11g
sudo docker image ls

sudo docker run --name oracle11g -d -p 1521:1521 jaspeen/oracle-xe-11g
sudo docker exec -it oracle11g bash

su – oracle(직접타이핑)
sqlplus / as sysdba
Enter password: oracle

alter user hr account unlock identified by hr;
conn hr/hr
select count(*) from employees;




sudo yum update : 새로운 패키지 설치전 업데이트

sudo yum install nodejs npm : 노드JS 설치

mkdir NodeJS
cd NodeJS/  : 노드 JS 작업공간 만든후 그폴더로 이동

npm init -y : 프로젝트 초기화

npm install express : express 설치
vi index.js 작성

node index.js : 서버실행

Ctrl + C : 서버에서 나오기

실행해보기


* 터미널1
실행중인 node 서버 종료
Ctrl+C
cd ~/NodeJS
vi app.js 작성후 :wq
node app.js



* 터미널2
sudo docker ps
sudo docker ps -a
sudo docker start my-nginx
sudo docker exec -it my-nginx bash

cd /usr/share/nginx/html
- index.html 작성 후 :wq

